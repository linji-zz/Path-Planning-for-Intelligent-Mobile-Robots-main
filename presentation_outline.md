# 基于 Cross-Attention Transformer 与耦合奖励的路径规划 DQN

## 组会汇报提纲

---

## Slide 1: 标题页

**基于 Cross-Attention Transformer 与耦合奖励的路径规划方法**

Path Planning for Intelligent Mobile Robots with Cross-Attention Transformer and Coupled Reward

汇报人 / Presenter: _______
日期 / Date: _______

---

## Slide 2: 研究背景

### 移动机器人路径规划

- 目标：在已知/未知环境中找到从起点到终点的无碰撞路径
- 传统方法：A*, D*, RRT — 依赖全局地图，计算量大
- 强化学习方法：DQN — 端到端学习，适应性强

### 现有 DQN 路径规划的不足

1. **奖励信号单一**：仅使用距离变化，缺乏方向引导和障碍物感知
2. **网络结构受限**：MLP 感知能力有限，历史信息利用不足
3. **动作建模粗糙**：8 个方向被当作独立类别，忽略空间几何关系

---

## Slide 3: 研究动机

### 三个改进方向

| 角度 | 问题 | 改进 |
|:----|:-----|:-----|
| 奖励函数 | 奖励信号单一 | **耦合奖励** — 距离 + 方向对齐 + 开阔度 |
| 网络架构 | MLP 感知弱 | **Cross-Attention Transformer** — 两分支时序建模 |
| 动作表示 | 动作被当独立类别 | **Direction-Aware Embedding** — 动作嵌入几何编码 |

### 三者关系

```
奖励塑形 → 更合理的引导信号
    ↓
Transformer → 更强的历史感知能力
    ↓
动作嵌入 → 更精细的动作价值估计
```

---

## Slide 4: 基线方法：原始 DQN

### 环境定义

- **栅格地图**：15 × 15，含 35 个障碍物
- **起点**：(1, 1)，**终点**：(13, 13)
- **动作**：8 方向移动（上、下、左、右、四个对角）
- **状态**：11 维向量 [dx, dy, dist, obs0~obs7]

### DQN 流程

```
状态 s (11维) → MLP (128→128→8) → Q(s,a) → ε-greedy 选动作 → 执行 → 更新
```

### 原始奖励

```
r = (old_dist - new_dist) × 2.0    # 距离变化
r_goal = 50.0                        # 到达终点
r_collision = -10.0                  # 碰撞
```

### 基线性能

- 成功率：~100%
- 平均路径：~14 步
- 局限：14 步已是该地图最优解，需要更难地图验证

---

## Slide 5: 贡献一：耦合奖励

### 动机

原始距离奖励仅考虑"目标远近"，忽略了：
- **方向引导**：朝目标走和背目标走应该区分
- **障碍物感知**：往开阔地带走更安全

### 耦合奖励设计

```
r = r_distance + r_alignment + r_openness
```

| 分量 | 公式 | 含义 | 权重 |
|:-----|:-----|:-----|:----:|
| 距离奖励 | (d_old - d_new) × 2.0 | 接近目标 | 基础 |
| 方向对齐 | 0.5 × cos(θ - θ_goal) | 子目标方向一致性 | 0.5 |
| 开阔度 | 0.3 × (1 - 障碍物占比/9) | 3×3 邻域安全 | 0.3 |

### 三个信号的互补关系

```
方向对齐 → 引导走向目标方向
    +
开阔度奖励 → 避免走向障碍物密集区
    =
    更安全的路径
```

---

## Slide 6: 贡献二：Cross-Attention Transformer-DQN

### 动机

现有问题：
1. **MLP 只看当前帧**，不知道历史轨迹走向
2. **状态被统一处理**，agent-goal 和 obstacle 信息性质不同却共用相同网络

### 核心设计

将 11 维状态拆成两个分支，分别处理后交叉融合：

```
输入 (seq_len=5, 每步 11 维)
         │
    ┌────┴────┐
    │         │
  AG 分支      OBS 分支
  [dx,dy,dist]  [obs0~obs7]
  (3 维)        (8 维)
    │             │
  FC(d_model)    FC(d_model)
    │             │
  Self-Attn      Self-Attn
  (看时序变化)   (看时序变化)
    │             │
    └── Cross ────┘
      Attention
    (AG 查 OBS)
         │
     取最后时间步 → FC → Q 值
```

### 为什么有效

| 分支 | 编码 | 要回答的问题 |
|:-----|:-----|:------------|
| Agent-Goal | dx, dy, dist | 我离目标多远？方向对吗？ |
| Obstacle | 8 个 obs 标志 | 哪里被挡住了？路通不通？ |

Cross-Attention：

> "目标在右前方（AG），但右前方有障碍（OBS），所以应前行再绕"

### 与 MLP 的对比

| 能力 | MLP | Cross-Attn Transformer |
|:-----|:---:|:----------------------:|
| 历史感知 | ❌ 只看当前 | ✅ 看 5 步历史 |
| 空间推理 | ❌ 混合处理 | ✅ 分支编码 + 交叉融合 |
| 泛化能力 | ❌ 易过拟合 | ✅ 注意力泛化更好 |

---

## Slide 7: 贡献三：Direction-Aware Action Embedding

### 动机

当前 DQN 把 8 个方向动作当作**独立类别**：

```
Q(s,a₀) = W₀ · h + b₀
Q(s,a₁) = W₁ · h + b₁
...

→ 网络不知道 a₀ (-1,-1) 和 a₇ (1,1) 在空间上是相邻的
```

### 改进

把动作映射为一个 **2 维空间向量** (dx, dy)，显式编码方向信息：

```python
action_embed = Embedding(8, 2)   # 学到 (dx, dy) 表示
Q(s,a) = FC(concat(h, action_embed(a)))  # 单头输出
```

### 优势

1. **几何先验**：网络学到方向连续性
2. **参数共享**：单头头替换 8 头，效率提升
3. **可解释性**：可视化学到的动作嵌入，看物理意义
4. **泛化**：未见的相似状态也能正确推理

### 已有基础

- Action Representation Learning（Tavakoli 2018, Chandak 2019）
- 在**栅格路径规划的 8 方向离散动作上做嵌入**目前较少见

---

## Slide 8: 实验设计

### 对比实验

| 实验编号 | 奖励 | 网络 | 动作表示 |
|:--------:|:----|:----|:---------|
| Baseline | 原始距离 | MLP | 独热编码 |
| Exp 1 | **耦合奖励** | MLP | 独热编码 |
| Exp 2 | 耦合奖励 | **Cross-Attn Transformer** | 独热编码 |
| Exp 3 | 耦合奖励 | Cross-Attn Transformer | **Action Embedding** |

### 评估指标

- **成功率** (Success Rate)
- **路径长度** (Path Length)
- **收敛轮数** (Convergence Episode)
- **泛化能力** (Generalization to new maps)

### 地图设置

- 训练：固定 15×15 地图
- 测试：同地图 + 随机障碍物地图（泛化测试）

---

## Slide 9: 进度与计划

### 已完成

- ✅ 基线 DQN 训练与评估（100% SR）
- ✅ 耦合奖励实现（离散环境验证通过）
- ✅ DWA 集成尝试（已评估并确认不适合当前方向）

### 进行中

- 🔄 Cross-Attention Transformer-DQN 编码
- 🔄 Action Embedding 模块设计

### 待完成

- ⬜ 三组对比实验训练与评估
- ⬜ 泛化测试（随机障碍物地图）
- ⬜ 实验结果整理与图表制作
- ⬜ 论文写作

---

## Slide 10: 讨论

### 当前思考

1. **基线过于饱和** — 15×15 地图上 DQN 已达 100%/14 步最优解，改地图难度是下一步关键
2. **三贡献层次清晰** — 奖励函数 / 网络架构 / 动作表示，三个维度不重叠
3. **DWA 放弃决策** — DWA 在仿真中性能下降，但论文可讨论其在实际机器人上的必要性

### 待解决的问题

1. 是否需要更大/随机地图来拉开差距？
2. 三篇是否能构成完整的小论文故事线？
3. Action Embedding 的新颖性是否足够？

---

*谢谢！欢迎提问讨论*
