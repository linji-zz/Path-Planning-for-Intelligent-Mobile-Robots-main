# 本文方法

---

## 1. 耦合奖励函数

### 1.1 问题分析

在 DQN 路径规划中，奖励函数是引导智能体学习策略的核心信号。传统 DQN 算法通常采用基于距离变化的奖励函数：

$$r = (d_{t-1} - d_t) \times 2.0$$

其中 $d_t$ 为 $t$ 时刻机器人与目标点的欧氏距离。当 $d_{t-1} > d_t$ 时，机器人获得正奖励，反之获得负奖励。

该奖励函数存在以下不足：

**（1）缺乏方向引导。** 距离变化仅反映"是否接近目标"，无法区分"正面接近"和"绕路接近"。如图 X(a) 所示，当机器人在障碍物前向右绕行时，虽然每一步都在接近目标（距离减小），但绕行路径明显比直接左转更长。然而，距离奖励对这两种路径的评估是相同的，无法引导机器人选择更优的方向。

**（2）缺乏障碍物感知。** 距离奖励仅考虑目标点，完全忽略了障碍物分布。如图 X(b) 所示，当机器人位于障碍物密集区域时，距离奖励仍可能为正（因为正在接近目标），但实际选择的动作方向可能指向障碍物密集区域，增加了碰撞风险。

**（3）奖励稀疏。** 当机器人距离目标较远时，每一步的距离变化量较小，导致奖励值接近零，无法有效区分不同动作的优劣。

现有研究针对上述问题进行了改进。李奇儒等[9]结合机器人移动方向与目标点距离设计奖励函数，在靠近目标时给予正奖励，远离时给予负奖励。田箫源等[10]利用状态间访问次数差异给予奖励，平衡状态访问频率。温海骏等[11]设计了包含距离变化、分层奖励、运动奖励及事件完成奖励的复合奖励函数。然而，上述方法均未考虑局部邻域的障碍物分布对路径规划的影响。

### 1.2 耦合奖励设计

针对上述问题，本文提出一种耦合奖励函数（Coupled Reward Function），将距离奖励、方向对齐奖励与局部开阔度奖励三者耦合，从三个维度综合评估每一步动作的优劣。

#### 1.2.1 距离奖励

距离奖励衡量机器人在当前步中接近目标的程度，其定义如下：

$$r_{dist} = (d_{t-1} - d_t) \times \beta$$

其中 $\beta$ 为距离奖励系数，本文取 $\beta = 2.0$。该奖励保留了传统距离奖励的核心思想——鼓励机器人趋近目标。

#### 1.2.2 方向对齐奖励

方向对齐奖励评估机器人当前朝向与子目标方向之间的一致性，其定义如下：

$$r_{align} = \alpha \cdot \cos(\theta - \theta_{goal})$$

其中 $\alpha$ 为方向对齐系数，本文取 $\alpha = 0.5$；$\theta$ 为机器人当前的移动方向角；$\theta_{goal}$ 为从机器人当前位置指向子目标的方向角。

该奖励的计算过程为：根据动作 $a$ 确定子目标位置 $p_{sub} = p_{agent} + (dx, dy)$，计算子目标相对于机器人当前位置的方向角 $\theta_{goal} = \arctan2(y_{sub} - y_{agent}, x_{sub} - x_{agent})$，然后与机器人实际移动方向 $\theta$ 计算夹角余弦。当机器人朝向子目标方向移动时，$\cos(\theta - \theta_{goal}) \approx 1$，获得正向奖励；当机器人背离子目标方向移动时，获得负向奖励。

与距离奖励的区别在于：距离奖励只关注"是否更近"，而方向对齐奖励关注"是否走对了方向"。如图 X 所示，即使两步移动的距离变化相同，朝向目标方向移动的一步获得的额外奖励大于偏离方向的一步。

#### 1.2.3 局部开阔度奖励

局部开阔度奖励评估子目标周围区域的障碍物密度，引导机器人选择障碍物稀疏的安全路径，其定义如下：

$$r_{open} = \gamma \cdot (1 - \frac{N_{obs}}{9})$$

其中 $\gamma$ 为开阔度系数，本文取 $\gamma = 0.3$；$N_{obs}$ 为子目标周围 $3 \times 3$ 邻域内的障碍物网格数。

该奖励的计算过程为：对于子目标 $(x_{sub}, y_{sub})$，统计其周围 $3 \times 3$ 邻域（共 9 个网格）中的障碍物数量 $N_{obs}$，然后计算开阔度 $openness = 1 - N_{obs}/9$。当子目标周围无障碍物时，$openness = 1$，获得最大奖励 $0.3$；当子目标周围充满障碍物时，$openness = 0$，奖励为零。

如图 X 所示，开阔度奖励的作用是引导机器人选择"虽然当前步距离变化不大，但下一步选择更多"的安全方向，避免走入死胡同。

#### 1.2.4 总奖励

最终的耦合奖励函数为：

$$r = r_{dist} + r_{align} + r_{open}$$

对于终止状态（到达目标或碰撞），使用固定奖励覆盖：到达目标 $r = 50.0$，碰撞或出界 $r = -10.0$。

### 1.3 各奖励分量的作用分析

三个奖励分量从不同角度引导机器人的学习过程，它们之间的互补关系如表 1 所示。

表 1 三个奖励分量的互补关系

| 奖励分量 | 引导目标 | 解决什么问题 | 取值范围 |
|:---------|:---------|:------------|:--------:|
| 距离奖励 $r_{dist}$ | 趋近目标 | 保证路径收敛到目标 | $[-2\Delta d_{max}, 2\Delta d_{max}]$ |
| 方向对齐 $r_{align}$ | 选择正确方向 | 避免迂回路径 | $[-0.5, 0.5]$ |
| 开阔度 $r_{open}$ | 避开障碍密集区 | 提高路径安全性 | $[0, 0.3]$ |

距离奖励提供最基本的趋近目标的动力，是奖励函数的主干。方向对齐奖励在主干基础上增加了方向选择的正向激励，避免走"先远离再折返"的低效路径。开阔度奖励在安全维度上进一步约束，引导机器人避开障碍物密集区域。

三个奖励分量的权重系数（$\beta=2.0, \alpha=0.5, \gamma=0.3$）反映了各分量的重要程度差异：距离变化是奖励信号的主成分，方向对齐辅助引导方向选择，开阔度作为安全性的软约束。消融实验（见第 X 节）验证了各分量的有效性。

---

## 2. Cross-Attention Transformer DQN

### 2.1 问题分析

传统 DQN 采用多层全连接网络（MLP）作为 Q 网络，其输入为当前时刻的 11 维状态向量 $s = [dx, dy, dist, obs_0, obs_1, \dots, obs_7]$，输出为 8 个动作的 Q 值。这种处理方式存在以下问题：

**（1）两类信息混合编码。** 11 维状态包含两类性质完全不同的信息：目标位置信息（$dx, dy, dist$）和障碍物信息（$obs_0 \sim obs_7$）。前者描述"目标在哪、多远"，后者描述"哪个方向被挡住了"。MLP 将它们通过同一组参数进行编码，无法区分这两个不同的推理任务。

**（2）缺乏时序建模能力。** MLP 仅处理当前时刻的状态，无法利用历史轨迹信息。在路径规划中，一些决策需要参考历史信息（例如，进入狭窄通道需要判断之前几步的运动趋势），MLP 的"无记忆"特性限制了其决策质量。

**（3）动作方向关系被忽略。** 8 个输出头对应 8 个方向动作，但这些动作之间的空间关系（如 $a_0(-1,-1)$ 与 $a_1(-1,0)$ 在方向上是相邻的）未被编码。

近年来，将 Transformer 引入 DQN 成为改进方向之一。Sharma 等[6]提出了 Transformer-Enhanced DQN，采用多级自注意力和笛卡尔位置编码处理状态序列，增强了对长程空间依赖的建模能力。张君等[8]将 Transformer 结构融入 DQN 框架，提升了模型对动态环境的推理能力。Cheng 等[7]设计了一种多级注意力 DQN，使用 4 个专门的注意力头提取威胁环境中的不同特征。

然而，上述方法均采用**单通路自注意力架构**，将完整的状态向量直接输入自注意力层，未对目标信息和障碍物信息进行区分编码。事实上，在路径规划的认知过程中，"目标在哪"和"哪里被挡住"是两个本质不同的子问题，应该分别处理后再综合决策。

### 2.2 网络结构设计

针对上述问题，本文提出 Cross-Attention Transformer DQN（CAT-DQN）。其核心思想是将状态空间分离——目标信息和障碍物信息分别编码，再通过交叉注意力机制融合。网络结构如图 X 所示。

#### 2.2.1 两分支编码

给定一个包含 $L$ 个时间步的历史状态序列 $S = [s_{t-L+1}, s_{t-L+2}, \dots, s_t] \in \mathbb{R}^{L \times 11}$，首先将其沿特征维度拆分为两个子序列：

$$S_{AG} = [dx, dy, dist] \in \mathbb{R}^{L \times 3}$$
$$S_{OBS} = [obs_0, obs_1, \dots, obs_7] \in \mathbb{R}^{L \times 8}$$

两个子序列分别经过线性投影映射到 $d_{model}$ 维隐空间：

$$H_{AG} = S_{AG} W_{AG} + b_{AG}, \quad W_{AG} \in \mathbb{R}^{3 \times d_{model}}$$
$$H_{OBS} = S_{OBS} W_{OBS} + b_{OBS}, \quad W_{OBS} \in \mathbb{R}^{8 \times d_{model}}$$

其中 $d_{model} = 64$。线性投影后，两个分支各自经过一层 Transformer Encoder Layer（Self-Attention + Feed Forward），分别捕捉各自分支在时间序列上的变化规律：

$$H^{(1)}_{AG} = \text{TransformerEncoder}(H_{AG})$$
$$H^{(1)}_{OBS} = \text{TransformerEncoder}(H_{OBS})$$

Self-Attention 的物理含义为：AG 分支学习历史步中目标位置和距离的变化趋势，OBS 分支学习各方向障碍物标志在时间维度上的演变模式。

#### 2.2.2 交叉注意力融合

在 Self-Attention 编码后，通过交叉注意力（Cross-Attention）层实现两路信息的融合。具体地，AG 分支的输出作为 Query，OBS 分支的输出作为 Key 和 Value：

$$Q = H^{(1)}_{AG} W_Q, \quad K = H^{(1)}_{OBS} W_K, \quad V = H^{(1)}_{OBS} W_V$$

$$\text{CrossAttn}(Q, K, V) = \text{Softmax}\left(\frac{QK^T}{\sqrt{d_k}}\right) V$$

交叉注意力的含义为：**AG 分支根据"目标在哪"（Query）去查询 OBS 分支的"障碍物分布"（Key-Value），决策时综合考虑目标方向与障碍物约束。**

交叉注意力的输出与原始 AG 分支输出做残差连接和层归一化：

$$H_{fused} = \text{LayerNorm}(H^{(1)}_{AG} + \text{CrossAttn}(Q, K, V))$$

取序列最后一个时间步的隐状态 $h_t = H_{fused}[-1, :]$，经全连接层输出 8 个动作的 Q 值：

$$Q(s, a) = h_t W_{out} + b_{out}, \quad W_{out} \in \mathbb{R}^{d_{model} \times 8}$$

#### 2.2.3 与 Self-Attention 的对比

为了说明 Cross-Attention 的优势，表 2 对比了 Self-Attention 和 Cross-Attention 在本问题中的区别。

表 2 Self-Attention 与 Cross-Attention 的对比

| | Self-Attention（已有方法） | Cross-Attention（本文） |
|:--|:-------------------------|:----------------------|
| 输入 | 11 维混合状态 | 3 维 AG + 8 维 OBS 分别编码 |
| Q来源 | 自己 | AG 分支 |
| K,V来源 | 自己 | OBS 分支 |
| 物理含义 | "状态各维度之间的相关性" | "目标信息去查询障碍物信息" |
| 推理模式 | 被动关联 | **主动查询** |

Self-Attention 处理的是混合编码后的特征向量，各维度之间的相关性是模糊的——无法区分"目标位置"和"障碍物"两类信息之间的交互。而 Cross-Attention 建立了明确的"查询—检索"关系：AG 分支主动向 OBS 分支询问"我打算往这个方向走，你那边有没有阻挡"，这种推理模式更符合路径规划的实际决策过程。

### 2.3 超参数

CAT-DQN 的主要超参数设置如下：序列长度 $L = 5$，隐层维度 $d_{model} = 64$，注意力头数 $n_{head} = 4$，Transformer 层数 $N_{layer} = 1$。时间步嵌入采用可学习的位置编码。上述参数的选取依据见第 X 节的消融实验。

---

## 3. 软性动作屏蔽机制

### 3.1 问题分析

在 DQN 路径规划中，状态向量的后 8 维 $obs_0 \sim obs_7$ 直接编码了 8 个方向是否存在障碍物。其中 $obs_i = 1$ 表示第 $i$ 个方向存在障碍物，$obs_i = 0$ 表示第 $i$ 个方向安全。这些标志直接反映了各个方向的可通行状态。

然而，传统 DQN 仅将这些标志作为输入特征，未在动作选择阶段显式利用这一信息。网络需要通过与环境的交互试错，自主建立"$obs_i = 1 \Rightarrow$ 动作 $a_i$ 会导致碰撞"的关联。这一学习过程通常需要经历以下阶段：

1. **初期探索阶段**：网络随机选择动作，频繁撞墙
2. **经验积累阶段**：网络从 replay buffer 中采样到大量 $(state, action_{collision}, -10.0, done)$ 的经验
3. **关联建立阶段**：网络逐渐降低在 $obs_i = 1$ 的状态下选择动作 $a_i$ 的概率

这一过程通常需要数千步的交互和数百次碰撞，且在新环境中需要重新学习。图 X 显示了传统 DQN 训练过程中碰撞次数的分布：前期碰撞密集，后期逐渐减少。

现有研究对动作选择进行了改进。温海骏等[11]引入人工势场法引导动作选取，利用引力场和斥力场的合力方向指导动作方向。张君等[8]将 APF 势场信息集成至 DQN 框架。然而，APF 方法存在以下不足：（1）需要额外计算引力场和斥力场，增加了计算开销；（2）APF 对局部最优问题敏感，在复杂障碍物环境中容易陷入死胡同；（3）APF 的引导与 DQN 的策略学习相互独立，两者的融合需要额外的超参数调优。

### 3.2 软性动作屏蔽设计

针对上述问题，本文提出软性动作屏蔽机制（Soft Action Masking, SAM），其核心思想是：**状态中已经包含了 $obs$ 标志这一显式的方向可通行信息，无需网络通过大量试错来学习，直接在动作选择阶段加以利用。**

具体地，在 Q 网络前向推理得到各动作的原始 Q 值后，根据 $obs$ 标志对相应动作的 Q 值进行修正：

$$Q"(s, a_i) = Q(s, a_i) - \lambda \cdot obs_i$$

其中 $\lambda$ 为屏蔽强度系数，本文取 $\lambda = 10.0$。修正后的 Q 值 $Q"(s, a_i)$ 用于动作选择：

$$a^* = \arg\max_i Q"(s, a_i)$$

在训练阶段，原始 Q 值 $Q(s, a_i)$ 仍用于计算 TD 误差和损失函数，以确保网络本身能够维持对所有动作的合理估值。

#### 3.2.1 软屏蔽与硬屏蔽的区别

硬屏蔽（Hard Masking）将不安全动作的 Q 值设为 $-\infty$，彻底排除被选择的可能：

$$Q"(s, a_i) = \begin{cases} -\infty, & obs_i = 1 \\ Q(s, a_i), & obs_i = 0 \end{cases}$$

软屏蔽则保留一定程度的探索可能：

$$Q"(s, a_i) = Q(s, a_i) - \lambda \cdot obs_i$$

两者的区别在于：硬屏蔽完全杜绝了不安全动作的选择，适用于环境完全已知、传感器完全可靠的情况。软屏蔽保留了 $\lambda$ 这一可控参数，当所有安全方向的 Q 值均较低时（例如训练初期），不安全方向仍有可能被选择，这为探索提供了额外的通道。

#### 3.2.2 与 APF 引导的对比

与 APF 引导相比，软性动作屏蔽具有以下优势：

1. **零额外计算开销**：$obs$ 标志已经是状态的一部分，屏蔽操作仅为一次向量减法，无需计算引力场和斥力场。
2. **与 DQN 原生兼容**：屏蔽作用于动作选择阶段，不影响 Q 网络的训练和更新，无需额外的融合机制。
3. **可调节性**：屏蔽强度 $\lambda$ 提供了平滑的调节手段，$\lambda = 0$ 退化为原始 DQN，$\lambda \to \infty$ 趋近于硬屏蔽。

### 3.3 实现细节

软性动作屏蔽的实现仅需在动作选择处添加屏蔽逻辑：

```
算法 1: 软性动作屏蔽

输入: 当前状态 s, Q 网络 Q_θ
输出: 选择的动作 a

1. q_values = Q_θ(s)                    // 获取原始 Q 值
2. obs = s[3:11]                        // 提取 obs 标志
3. q_masked = q_values - λ * obs        // 施加软屏蔽
4. a = argmax(q_masked)                 // 选择修正后 Q 值最大的动作
5. 返回 a
```

在训练时，经验存储使用原始 Q 值对应的动作效果，但实际执行的动作由 $q_{masked}$ 决定。由于 $q_{masked}$ 排除了不安全方向，网络采集的经验中碰撞比例显著降低，正向经验的比例提升，从而加速收敛。

$\lambda$ 的取值影响屏蔽的强度：取值过小（$\lambda < 5.0$）屏蔽效果不明显，不安全方向仍可能被频繁选择；取值过大（$\lambda > 20.0$）近似于硬屏蔽，丧失探索能力。经实验验证，$\lambda = 10.0$ 在屏蔽效果和探索保留之间取得了良好平衡。
