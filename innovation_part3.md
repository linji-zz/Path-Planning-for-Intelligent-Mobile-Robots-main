## 3. 自适应安全引导策略

### 3.1 问题分析

在 DQN 路径规划中，状态向量的后 8 维 obs_0 ~ obs_7 直接编码了 8 个方向是否存在障碍物。其中 obs_i = 1 表示第 i 个方向存在障碍物，obs_i = 0 表示该方向安全。这些标志蕴含了动作选择的先验知识——有障碍的方向应避免选择。

然而，传统 DQN 的训练过程分为两个阶段，均未有效利用这一信息：

（1）探索阶段（epsilon-greedy 随机选动作）。当智能体以概率 epsilon 进行随机探索时，所有 8 个方向被等概率选择。这意味着即使 obs_i = 1 指示第 i 个方向存在障碍物，该方向仍以 1/8 的概率被选中，导致大量无效碰撞。

（2）利用阶段（贪婪选动作）。当智能体以概率 1-epsilon 选择 Q 值最大的动作时，网络需要通过累计的碰撞经验逐步降低不安全方向的 Q 值。这一过程通常需要数千步的交互和数百次碰撞，收敛缓慢。

现有研究对动作选择过程进行了改进。田箫源等设计了动态探索因子，根据训练进度调整 epsilon 值，但未解决随机探索中的方向选择偏好问题。温海骏等引入人工势场法引导动作选取，但 APF 方法需要额外计算引力场和斥力场，且对局部最优问题敏感。马海杰等引入注意力机制增强网络对历史信息的利用，但该机制作用于网络内部的特征提取，未直接约束动作选择。

### 3.2 自适应安全引导策略设计

针对上述问题，本文提出自适应安全引导策略（Adaptive Safety-Guided Strategy, ASGS），从探索和利用两个阶段分别对动作选择过程进行安全引导。

#### 3.2.1 安全加权探索

在探索阶段，传统的等概率随机选择忽略了障碍物信息。本文提出安全加权探索机制，根据各方向的安全程度分配选择概率：

P(a_i) = w_i / sum(w_j),  w_i = 0.1 if obs_i=1, else 1.0

该机制下，安全方向被选择的概率远高于不安全方向，但保留了约 1% 的探索可能性，避免因过度屏蔽而遗漏潜在可行路径。

#### 3.2.2 自适应 Q 值修正

在利用阶段，本文提出自适应 Q 值修正机制，直接利用 obs 标志对 Q 值进行软性修正，修正强度随训练进度自适应调整：

Q'(s, a_i) = Q(s, a_i) - lambda * obs_i

其中 lambda 为修正强度系数，其取值根据训练进度动态调整。训练前期，网络对环境的认识尚不充分，lambda 取值较小（约为 5），允许选择不安全方向进行必要的探索；训练后期，网络已基本收敛，lambda 取值较大（约为 15），有效压制不安全方向，确保最终策略的安全性。lambda 的具体调整方式为：

lambda = 5 + 10 * (当前训练轮数 / 总训练轮数)

即轮数越靠后，lambda 越大，屏蔽越强。

### 3.3 算法流程

算法 1: 自适应安全引导策略

1. obs = s[3:11]                           // 提取 obs 标志
2. if random() < epsilon:                  // 探索阶段
3.     w = [0.1 if o==1 else 1.0 for o in obs]
4.     a = weighted_random_choice(w)       // 安全加权选择
5. else:                                    // 利用阶段
6.     lambda = 5 + 10 * k / K             // 自适应强度
7.     q = Q(s) - lambda * obs             // Q 值修正
8.     a = argmax(q)
9. 返回 a

ASGS 的实现仅需在动作选择处添加约 5 行代码，不改变 DQN 的训练流程和网络结构，与本文提出的耦合奖励和 Cross-Attention Transformer 完全兼容。
